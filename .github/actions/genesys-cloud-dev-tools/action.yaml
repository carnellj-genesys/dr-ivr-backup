###
#  This action installs the installs archy.
#  It also adds Archy to the path
#
###
name: "Setup Archy  Cloud Tools"
description: "Installs Archy"
runs:
  using: "composite"
  steps:
    - run: |
        mkdir output
        mkdir archy
        cd archy
        wget -q https://sdk-cdn.mypurecloud.com/archy/latest/archy-linux.zip
        unzip archy-linux.zip
        . archy version                            
        export PATH=$PATH:$GITHUB_WORKSPACE/archy
        cd ..
        pwd
        archy version
      shell: bash
